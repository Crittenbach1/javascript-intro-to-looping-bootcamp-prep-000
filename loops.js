function forLoop(array) {
    for (let i = 0; i < 25; i++) {
        console.log(`I am ${i} strangle loops.`);
    }}

    function whileLoop(numb) {
        while(numb>0) {
          console.log(--numb);
        }
        console.log("done");
    }

    function doWhileLoop(array) {
    function maybeTrue() {
        return Math.random() >= 0.5
}
    do {
        array.pop();
    } while (maybeTrue());

      console.log(array); }

done()
